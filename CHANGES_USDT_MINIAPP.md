# به‌روزرسانی Saraf — ربات مدیریت جدا + مینی‌اپ خرید/فروش تتر

این بسته مستقیماً روی آخرین نسخهٔ ریپازیتوری `sjdpluse/saraf` (کلون‌شده از گیت‌هاب)
اعمال شده و از نظر import و اجرا کامل تست شده است. کافیست محتوای این پوشه را روی
پوشهٔ پروژهٔ محلی‌ات کپی/جای‌گزین کنی و commit بزنی.

## خلاصهٔ همهٔ تغییرات

### فایل‌های تغییریافته
- `config.py` — افزودن `ADMIN_BOT_TOKEN`، `SUPPORT_TELEGRAM_USERNAME`، `MINI_APP_URL`، `USDT_RECEIPTS_BUCKET`
- `keyboards.py` — کیبوردهای جدید (انتخاب صرافی خرید، درخواست شماره تماس، تایید/رد ادمین) + دکمهٔ WebApp در منوی تتر
- `handlers/usdt.py` — بازنویسی کامل: اطلاع‌رسانی از طریق ربات مدیریت جدا، دریافت الزامی شماره تماس، انتخاب صرافی/کیف‌پول مقصد در خرید
- `bot.py` — مسیریابی پیام‌های جدید (شماره تماس، Contact)
- `api.py` — رفع باگ از‌قبل‌موجود (ایمپورت ماژول‌های حذف‌شدهٔ `gold_rate_engine`/`gold_market_service`) + endpointهای کامل مینی‌اپ + سرو کردن فایل‌های build
- `services/supabase_service.py` — توابع جدید: `get_usdt_orders_by_chat_id`, `upload_usdt_receipt`
- `requirements.txt` — افزودن `python-multipart` (لازم برای آپلود فایل در FastAPI)
- `Procfile` — افزودن سرویس `admin_worker`

### فایل‌های جدید
- `admin_bot.py` — ربات مستقل مدیریت (تایید/رد سفارش با یک لمس)
- `services/usdt_order_service.py` — منبع واحد ثبت سفارش (استفاده‌شده هم توسط ربات، هم API)
- `services/webapp_auth.py` — اعتبارسنجی امضای initData تلگرام
- `webapp/` — مینی‌اپ کامل (React + Vite)، از قبل build شده در `webapp/dist/`
- `sql/2026_08_usdt_miniapp_update.sql` — مهاجرت پایگاه داده
- `.env.example` — نمونهٔ متغیرهای محیطی

## ⚠️ یک باگ از قبل موجود که اصلاح شد
در کامیت «back to v2» دو فایل `services/gold_rate_engine.py` و
`services/gold_market_service.py` حذف شده بودند اما `api.py` هنوز به آن‌ها ارجاع
می‌داد — یعنی سرویس `web` روی Railway در حال کرش بود. این را با بازگرداندن منطق
محاسبهٔ طلا به `gold_service.py` (همان چیزی که ربات هم استفاده می‌کند) اصلاح کردم؛
الان `/api/gold` و `/api/gold/{karat}` دوباره کار می‌کنند.

## مراحل دیپلوی (به ترتیب انجام بده)

### ۱) پایگاه داده
اسکریپت `sql/2026_08_usdt_miniapp_update.sql` را در Supabase SQL Editor اجرا کن.
سپس طبق راهنمای داخل همان فایل، یک باکت Storage عمومی به نام `usdt-receipts` بساز.

### ۲) ساخت ربات مدیریت
در تلگرام به @BotFather پیام بده، `/newbot` بزن، یک ربات کاملاً جدید بساز (مثلاً
`SarafAdminBot`) و توکنش را در `.env` به‌عنوان `ADMIN_BOT_TOKEN` قرار بده.

### ۳) متغیرهای محیطی
فایل `.env.example` را ببین و مقادیر واقعی (`ADMIN_BOT_TOKEN`, `MINI_APP_URL` و...)
را در Railway → Variables برای هر سه سرویس (`worker`, `admin_worker`, `web`) تنظیم کن.
`MINI_APP_URL` باید همان آدرس عمومی سرویس `web` باشد (مثلاً
`https://saraf-production.up.railway.app`) — این را بعد از اولین دیپلوی از تب
Settings → Networking همان سرویس در Railway پیدا می‌کنی.

### ۴) افزودن سرویس admin_worker در Railway
روی Railway، یک سرویس جدید در همان پروژه بساز که از همین ریپازیتوری، با
Start Command برابر `python admin_bot.py` اجرا شود (یا مطمئن شو Railway
`Procfile` را می‌خواند و پردازهٔ `admin_worker` را خودش می‌سازد).

### ۵) اتصال اولیهٔ ربات مدیریت
بعد از دیپلوی، با همان اکانت تلگرامی‌ای که چت‌آیدی‌اش در `ADMIN_CHAT_IDS` هست، به
ربات مدیریت پیام `/start` بفرست (تلگرام اجازه نمی‌دهد رباتی به کاربری که مکالمه
را شروع نکرده پیام بدهد).

### ۶) دیپلوی نهایی
بعد از تنظیم `MINI_APP_URL`، سرویس `web` را یک‌بار دیگر Redeploy کن تا دکمهٔ
مینی‌اپ در منوی تتر ظاهر شود (اگر `MINI_APP_URL` خالی باشد، ربات به‌صورت خودکار
فقط جریان گفتگویی معمولی را نشان می‌دهد — هیچ‌چیزی خراب نمی‌شود).

### ۷) (اختیاری ولی پیشنهادی) تنظیم دکمهٔ منوی تلگرام
در @BotFather → ربات اصلی → Bot Settings → Menu Button → آدرس مینی‌اپ
(`https://<your-app>.up.railway.app/miniapp/`) را ثبت کن تا کاربران بتوانند از
دکمهٔ کنار جعبهٔ پیام هم مستقیم وارد اپ شوند.

## توسعهٔ محلی مینی‌اپ (در صورت نیاز به تغییر بعدی)
```bash
cd webapp
npm install
npm run dev        # پیش‌نمایش محلی روی پورت 5173 (با پراکسی به /api روی 8000)
npm run build      # ساخت نسخهٔ نهایی در webapp/dist/ — این پوشه باید commit شود
```
`webapp/dist/` را حتماً commit کن، چون سرویس `web` مستقیماً همین فایل‌های
build-شده را سرو می‌کند و Railway به‌صورت پیش‌فرض مرحلهٔ build فرانت‌اند را اجرا
نمی‌کند.

## تست‌هایی که همین‌جا قبل از تحویل انجام شد
- سینتکس تمام فایل‌های پایتون تغییریافته ✅
- import کامل `bot.py`, `api.py`, `admin_bot.py` با متغیرهای محیطی نمونه ✅
- `build_application()` ربات اصلی (۲۹ handler) ✅
- اجرای واقعی `uvicorn api:app` و تست زندهٔ endpointها:
  - `/api/health` → 200 ✅
  - `/miniapp/` → 200 (فایل‌های React سرو می‌شوند) ✅
  - `/api/usdt/quote` بدون initData → 401 (رد صحیح) ✅
  - `/api/usdt/quote` با initData معتبر (امضای HMAC شبیه‌سازی‌شده) → عبور از احراز هویت ✅
  - `/api/usdt/orders/me` با initData معتبر → 200 ✅
- `npm run build` مینی‌اپ بدون خطا ✅

---

## دور دوم ارتقاء — بازطراحی «Vault Dark» + قابلیت‌های اعتمادسازی واقعی

### چه چیزی تغییر کرد

**۱) Timeline واقعی وضعیت سفارش (نه فقط یک برچسب رنگی)**
جریان تایید سفارش حالا سه مرحله‌یی است، هرکدام با تایم‌استمپ واقعی در پایگاه داده:
`ثبت شد (created_at)` → `تایید شد (confirmed_at)` → `تکمیل شد (completed_at)`.
در ربات مدیریت (`admin_bot.py`)، بعد از زدن «تایید سفارش»، دکمهٔ جدید «📦 تکمیل شد»
جایگزین می‌شود — این دکمه فقط وقتی زده شود که ادمین واقعاً تتر/پول را ارسال کرده
باشد. در مینی‌اپ، صفحهٔ جزئیات هر سفارش این Timeline را با آیکون و رنگ مناسب هر
مرحله نمایش می‌دهد.

**۲) سیستم امتیازدهی واقعی**
وقتی ادمین سفارشی را «تکمیل شد» می‌زند، هم در ربات اصلی (پیام با دکمه‌های ⭐️۱ تا ⭐️۵)
و هم در مینی‌اپ (صفحهٔ جزئیات سفارش)، از مشتری خواسته می‌شود امتیاز بدهد. امتیازها
در ستون‌های جدید `rating`, `rating_comment`, `rated_at` ذخیره می‌شوند.

**۳) آمار اعتمادساز واقعی (نه ادعایی)**
`GET /api/usdt/stats` تعداد واقعی معاملات تکمیل‌شده و میانگین امتیاز را از پایگاه
داده محاسبه می‌کند و در صفحهٔ اصلی مینی‌اپ نمایش می‌دهد — فقط وقتی حداقل یک
معامله تکمیل شده باشد (تا عدد صفر یا گمراه‌کننده نمایش داده نشود).

**۴) صفحهٔ قوانین و شرایط (`/terms` در مینی‌اپ)**
شفافیت کامل دربارهٔ محدودهٔ معامله، کارمزد، زمان تحویل، مسئولیت آدرس اشتباه،
سیاست انصراف و بازگشت وجه.

**۵) بازطراحی کامل بصری — سیستم طراحی «Vault Dark»**
- پالت رنگی جدید: سرمه‌یی (`#0F172A`/`#1E3A5F`) + سبز اعتماد (`#059669`) برای
  خرید/موفقیت + قرمز هشدار (`#DC2626`) برای فروش/خطا — به‌جای طلایی قبلی که حس
  «تبلیغاتی» می‌داد.
- فونت `Inter` برای همهٔ اعداد، مبالغ و آدرس‌های ولت (خوانایی و دقت بیشتر، مثل
  اپ‌های بانکی واقعی) + `Vazirmatn` برای متن دری.
- همهٔ ایموجی‌ها با آیکون‌های حرفه‌یی SVG از `@phosphor-icons/react` جایگزین شدند.
- انیمیشن‌های ظریف ورود (`fade+slide`) با تاخیر پلکانی روی کارت‌ها.
- Skeleton loading به‌جای اسپینر ساده در «سفارش‌های من».
- دکمهٔ کپی سریع برای شمارهٔ حساب، آدرس ولت و کد سفارش.

### فایل‌های جدید در این دور
- `webapp/src/components/StatusBadge.jsx`
- `webapp/src/components/OrderTimeline.jsx`
- `webapp/src/components/RatingStars.jsx`
- `webapp/src/components/CopyRow.jsx`
- `webapp/src/components/Skeleton.jsx`
- `webapp/src/pages/Terms.jsx`

### فایل‌های تغییریافتهٔ اضافی
- `sql/2026_08_usdt_miniapp_update.sql` — ستون‌های `confirmed_at`, `completed_at`,
  `cancelled_at`, `rating`, `rating_comment`, `rated_at`. **این اسکریپت را دوباره
  در Supabase اجرا کن**، حتی اگر بار قبل اجرا کرده باشی (از `if not exists`
  استفاده شده، پس بی‌خطر است).
- `services/supabase_service.py` — توابع `mark_usdt_order_confirmed`,
  `mark_usdt_order_completed`, `mark_usdt_order_cancelled`, `set_usdt_order_rating`,
  `get_usdt_stats`
- `keyboards.py` — `admin_order_complete_keyboard`, `usdt_rating_keyboard`
- `admin_bot.py` — جریان سه‌مرحله‌یی تایید/تکمیل
- `handlers/usdt.py` — `usdt_rate_callback` (دریافت امتیاز از مشتری در ربات اصلی)
- `bot.py` — ثبت handler امتیازدهی
- `api.py` — `/api/usdt/stats`, `POST /api/usdt/orders/{id}/rate`

### تست‌های این دور
- `npm run build` مینی‌اپ با کتابخانهٔ جدید آیکون → بدون خطا (۴۵۸۵ ماژول، خروجی نهایی
  ۲۳۸ کیلوبایت JS / ۶۹ کیلوبایت gzip) ✅
- import کامل `bot.py`, `api.py`, `admin_bot.py` با ۳۰ handler در ربات اصلی ✅
- تست زندهٔ `GET /api/usdt/stats` بدون نیاز به احراز هویت → 200 ✅
- تست زندهٔ `/miniapp/` با فونت Inter در HTML خروجی → 200 ✅

### ⚠️ یادت نرود
اسکریپت SQL به‌روزشده را در Supabase اجرا کن — بدون ستون‌های جدید (`confirmed_at`
و بقیه)، Timeline و امتیازدهی کار نمی‌کنند (خطای ساکت در لاگ سرور می‌بینی، نه کرش).

---

## دور سوم ارتقاء — KYC + Trust Profile + Risk Engine + کارت دیجیتال مشتری

این بزرگ‌ترین ارتقاء تا این‌جاست: سیستم صرافی از «فقط گرفتن شماره تماس» به یک
سیستم واقعی احراز هویت و مدیریت ریسک تبدیل شد.

### ۱) KYC (احراز هویت) — فقط یک‌بار برای هر کاربر
پیش از **اولین** سفارش (چه در ربات، چه در مینی‌اپ)، کاربر باید پروفایلش را کامل
کند: نام، نام خانوادگی، شمارهٔ تماس، اطلاعات پرداخت، عکس مدرک هویتی، و سلفی
(الزامی). این اطلاعات یک‌بار برای همیشه ذخیره می‌شوند؛ سفارش‌های بعدی فقط
جزئیات همان معامله را می‌پرسند.
- در ربات: `handlers/kyc.py` — جریان گفتگویی مرحله‌به‌مرحله.
- در مینی‌اپ: `webapp/src/pages/Kyc.jsx` — فرم چندمرحله‌یی، دقیقاً قبل از خرید/فروش
  اگر پروفایل ناقص باشد نمایش داده می‌شود (`App.jsx` این را با `startTransaction()`
  بررسی می‌کند).
- مدارک هویتی و سلفی در باکت **خصوصی** `usdt-kyc-docs` ذخیره می‌شوند (نه باکت
  عمومی رسیدها) — این عمدی است، چون مدرک هویت اطلاعات حساس است.
- بلافاصله بعد از ثبت، درخواست بررسی (همراه با خودِ دو عکس) به ربات مدیریت
  ارسال می‌شود با دکمه‌های «✅ تایید هویت» / «❌ رد هویت».

### ۲) Trust Profile — جدول `user_profiles`
هر کاربر یک پروفایل اعتبار دارد که بعد از هر معامله به‌روزرسانی می‌شود:
`kyc_status` (Pending 🟡 / Verified 🔵 / Trusted 🟢 / Restricted 🔴)، `trust_score`،
تعداد معاملات موفق/لغوشده، حجم کل معاملات (USDT)، تاریخ عضویت، آخرین معامله.

**فرمول Trust Score (کاملاً شفاف، نه جعبهٔ سیاه)**، در `config.py` قابل‌تنظیم:
- پایه: ۵۰ امتیاز بعد از تایید هویت
- +۲ امتیاز به‌ازای هر معاملهٔ موفق (سقف ۴۰ امتیاز)
- −۱۰ امتیاز به‌ازای هر معاملهٔ لغوشده/مشکوک
- +۱۰ امتیاز و ارتقا خودکار به 🟢 Trusted بعد از ۱۰ معاملهٔ موفق پیاپی

ادمین با دستور **`/profile <chat_id>`** در ربات مدیریت، خلاصهٔ کامل Trust Profile
هر کاربر را می‌بیند (دقیقاً فرمتی که خودت مثال زدی: نام، وضعیت، تعداد معاملات،
حجم، Trust Score). این خلاصه به‌صورت خودکار زیر هر اعلان سفارش هم نمایش داده
می‌شود تا لازم نباشد جدا آن را جستجو کنی.

### ۳) Risk Engine — `services/risk_engine.py`
قبل از هر سفارش، ریسک ارزیابی و در پیام اطلاع‌رسانی به ادمین با دلایل دقیق نمایش
داده می‌شود:
- 🟢 **پایین** — کاربر verified/trusted + سابقهٔ خوب + مبلغ معمولی → پردازش عادی
- 🟡 **متوسط** — هویت هنوز تایید نشده، یا کاربر کم‌تجربه + مبلغ بالا (پیش‌فرض
  ≥ ۱۰۰۰ USDT) → نیاز به بررسی دقیق‌تر
- 🔴 **بالا** — کاربر Restricted، یا ≥۳ معاملهٔ لغوشده، یا ≥۳ بار تغییر اطلاعات
  پرداخت → توقف و بررسی ویژه

همهٔ آستانه‌ها در `config.py` با نام‌های `USDT_RISK_*` قابل‌تغییرند، بدون نیاز به
دست‌زدن به منطق.

### ۴) کارت دیجیتال مشتری — `services/card_service.py`
بعد از **هر** سفارش (خرید یا فروش)، یک کارت ۱۲۰۰×۷۲۰ با طراحی «Vault Dark» ساخته
می‌شود: لوگوی Saraf، عکس سلفی کاربر (دایره‌یی)، نام، وضعیت KYC، جزئیات معامله
(مقدار، مبلغ، صرافی/شبکه، کد سفارش)، و یک QR از آدرس دیپازیت. این کارت هم برای
خودِ مشتری، هم برای همهٔ ادمین‌ها به‌صورت عکس در تلگرام ارسال می‌شود — مشتری
می‌تواند این کارت را هنگام مراجعهٔ حضوری به نمایندهٔ Saraf نشان دهد.

نکتهٔ فنی مهم: متن فارسی روی کارت با موتور **raqm** (داخل خودِ Pillow، بدون نیاز
به کتابخانهٔ جداگانه) رسم می‌شود تا حروف درست به‌هم متصل و راست‌به‌چپ نمایش داده
شوند — این را با رندر واقعی تست کردم (تصاویر نمونه را در فرآیند ساخت دیدم).

### ۵) جریان جدید خرید/فروش — «درخواست + اطلاعات بیشتر»
دکمهٔ قدیمی «✅ ادامه» با دو دکمه جایگزین شد:
- **«✅ درخواست خرید/فروش تتر»** — وارد جریان معامله می‌شود (و اگر پروفایل ناقص
  باشد، اول KYC را کامل می‌کند، بعد دقیقاً به همان‌جا برمی‌گردد).
- **«💬 اطلاعات بیشتر»** — مستقیم چت با `@SJDPLUS` را باز می‌کند.

### فایل‌های جدید در این دور
- `handlers/kyc.py`, `services/kyc_service.py`, `services/risk_engine.py`,
  `services/card_service.py`
- `webapp/src/pages/Kyc.jsx`

### فایل‌های تغییریافتهٔ این دور
- `sql/2026_08_usdt_miniapp_update.sql` — جدول `user_profiles` + ستون‌های
  `risk_level`, `risk_reasons`, `card_image_path` روی `usdt_orders` + راهنمای
  باکت‌های خصوصی جدید. **این اسکریپت را دوباره در Supabase اجرا کن.**
- `config.py` — تنظیمات KYC/Trust Score/Risk Engine
- `keyboards.py` — کیبوردهای KYC، دکمهٔ «درخواست + اطلاعات بیشتر»
- `handlers/usdt.py` — دروازهٔ KYC، حذف پرسش تکراری شماره تماس (از پروفایل خوانده می‌شود)
- `services/usdt_order_service.py` — اتصال Risk Engine + تولید/ارسال کارت
- `services/supabase_service.py` — توابع کامل Trust Profile
- `admin_bot.py` — دستور `/profile`، بررسی KYC، ثبت خودکار نتیجهٔ معامله در Trust Profile
- `api.py` — `/api/usdt/profile`, `POST /api/usdt/kyc`، گیت KYC روی ثبت سفارش
- `webapp/src/App.jsx`, `Home.jsx`, `Buy.jsx`, `Sell.jsx` — دروازهٔ KYC، حذف مرحلهٔ تکراری شماره تماس
- `requirements.txt` — افزودن `Pillow`, `qrcode`

### تست‌های این دور (همه واقعاً اجرا شدند، نه فقط نوشته شدند)
- ساخت واقعی کارت دیجیتال با Pillow + raqm — بررسی بصری خروجی، رفع باگ رندر
  متن فارسی (حروف جدا/نامرتب) و باگ شفافیت جعبه‌ها (که در ابتدا به‌اشتباه به‌جای
  نیمه‌شفاف، کاملاً سفید رسم می‌شد) ✅
- تست کامل با عکس سلفی نمونه (برش دایره‌یی + حلقهٔ رنگی دور آن) ✅
- `npm run build` مینی‌اپ با صفحهٔ KYC جدید — بدون خطا ✅
- تست زندهٔ `GET /api/usdt/profile` با initData معتبر → `{"kyc_complete": false, ...}` ✅
- تست زندهٔ `POST /api/usdt/orders/buy` بدون KYC کامل → 403 با پیام واضح (گیت
  سمت سرور، مستقل از رفتار فرانت‌اند) ✅
- import کامل و بدون خطای `bot.py` (۳۰ handler)، `admin_bot.py` (۴ handler)،
  `api.py` (۱۹ مسیر `/api/*`) ✅
- جست‌وجوی کامل کد برای اطمینان از حذف کامل مکانیزم قدیمی درخواست شماره تماس،
  بدون هیچ ارجاع باقی‌مانده ✅
